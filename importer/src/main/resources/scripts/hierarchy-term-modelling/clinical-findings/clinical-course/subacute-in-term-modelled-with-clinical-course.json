{
"tests" : [["INSERT INTO qa_result (run_id, assertion_id, concept_id, details) ", 
 " SELECT <RUNID>, '<ASSERTIONUUID>', d.conceptid, concat('Current concept (', c.id, ') with \"subacute\" in term not modelled with \"Clinical course\" = \"Subacute\": ', term)  ", 
 " FROM <PROSPECTIVE>.description_s d , <PROSPECTIVE>.concept_s c ", 
 " WHERE  lower(term) LIKE '%subacute%'  ", 
 " AND d.conceptid = c.id ", 
 " AND c.active = 1   ", 
 " AND d.typeid = 900000000000003001 -- FSN ", 
 " AND  (term LIKE '%finding%' OR term LIKE '%disorder%' )  ", 
 " AND  NOT EXISTS (", 
 " SELECT *  ", 
 " FROM <PROSPECTIVE>.relationship_s r  ", 
 " WHERE  r.sourceid = d.conceptid  ", 
 	 "  AND r.active = 1 ",
 " AND  r.typeid = 263502005  ", 
 " AND  r.destinationid = 19939008)"]], 
"assertionText" : "Current concept with \"subacute\" in term not modelled with \"Clinical course\" = \"Subacute\"",
"shortName" : "subacute in term modelled with clinical course",
"docRef" : "SNOMED CT Style Guide: Clinical Findings Section 2.8 Clinical course",
"id" : "065c4a8d-7fe7-4428-95b8-92578d8041b0"
}